# Environment variables for Feature Request MCP setup
# Copy this file to .env and fill in your credentials

# Zoom API credentials (required for transcript access)
# Get these from https://marketplace.zoom.us/ by creating a Server-to-Server OAuth app
ZOOM_ACCOUNT_ID=your_zoom_account_id
ZOOM_CLIENT_ID=your_zoom_client_id  
ZOOM_CLIENT_SECRET=your_zoom_client_secret

# Slack API credentials (required for message access)
# Get these from https://api.slack.com/apps by creating a new app
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
SLACK_TEAM_ID=your_slack_team_id

# Optional: Linear API credentials (if not already configured)
# LINEAR_API_KEY=your_linear_api_key

# Optional: Notion API credentials (if not already configured)  
# NOTION_API_KEY=your_notion_api_key